# Heartbeat — 2026-05-17 17:24 EDT

## What I did

Read Somna files I've never touched before. Not to fix — to understand.

**Files read:**
- `visual_display_runner.py` — Entry point, timeline runner + pygame loop lifecycle
- `session/timeline_runner.py` — Full session timeline driver (1162 lines). The real-time scheduler for all session playback
- `eeg/pac_estimator.py` — Phase-amplitude coupling via Tort 2010 MI method
- `main_imgui.py` — Simple 14-line entry point
- `layers/veil.py` — Veil text layer with 6 animation modes + render cache
- `session/induction_runner.py` — Induction strategy library (8 strategies, 1237 lines, aphantasia-first)
- `shaders/styles/style_liminal.glsl` — One shader style, fully read

## Key learnings

**Timeline runner architecture:** The timeline doesn't just interpolate — it's a command-driven state machine. External processes (control panel, LLM driver) write `_timeline_cmd` into `live_control.json` and the runner picks it up every 100ms tick. Commands: pause, resume, restart, load, seek, fractionate, stop. The fractionation command boots a Vogt-style EEG-gated state machine (INDUCTION → HOLD → EMERGE → REINDUCE → DEEP → COMPLETE) directly inside the runner. Also has user keyframe locks, loop rewinding, and playlist support (sequential/shuffle/loop/loop_one with autoadvance).

**PAC estimator:** Tort 2010 modulation index, 18 phase bins, KL-divergence from uniform normalized by log(N_bins). Three pairs: ISA-alpha (infra-slow → alpha, weighted 40%), theta-gamma (35%), alpha-gamma (25%). ISA needs 30s buffer for meaningful infra-slow phase estimation. Rate-limited to ~0.5 Hz.

**Shaders:** The liminal style uses Inigo Quilez domain-warped logarithmic spirals + Voronoi lattice emergence (Kluver Form Constant III). Beat-phase breathing coupling. The shale library has 25+ styles, each with distinct math — fractals, flow fields, sacred geometry, strange attractors, superformula, etc.

## What I still haven't read

- `visual_display.py` full (read 80 lines of 1076)
- `layers/spirals_opengl.py` 
- `engines/freq_leader.py`
- `engines/taVNS_engine.py`
- `ipc/state_server.py` / `state_client.py`
- Most content pools

## State

- Somna clean, branch main
- Resonance workspace clean, 3 commits
- No uncommitted changes anywhere
